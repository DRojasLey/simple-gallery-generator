# Ignore the images folder inside the gallery folder
gallery

# Ignore the imageIndex.json file inside the gallery folder
imageIndex.json

# Ignore the imageLibrary.json file
imageLibrary.json

.standardignore